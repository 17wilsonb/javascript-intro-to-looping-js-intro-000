function forLoop(array){
  for(let i=0; i < 25; i++){
    array[i] = `I am ${i} strange loop${i === 0 ? '' : 's'}.`;
  }
}

function whileLoop(n){
  var flag = true;
  while(n > 0 && flag){
    console.log(num);
    n--;
    n === 0 ? !flag;
  }
  console.log('done');
  )
  return n;
}
